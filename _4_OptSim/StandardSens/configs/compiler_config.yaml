boblib_path: ../../../_0_Utils/external/BobLib/BobLib/package.mo

# Each worker runs its own OMC process. OMC is already multithreaded, so keep this low.
max_workers: 8

standards:
  SteadyStateEval:
    model: BobLib.Experiments.Standards.VehicleSim
    # Simulation settings: _3_StandardSim/SteadyStateEval/steady_state_eval_config.yml

batch:
  max_workers: 1
  timeout: 500
